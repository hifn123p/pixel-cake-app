<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Photo Picker(系统选择器) 与 SAF(ACTION_OPEN_DOCUMENT) 均由系统代持访问权，
         不需要 READ_MEDIA_IMAGES / READ_MEDIA_VISUAL_USER_SELECTED（FIX_LIST F19）。
         不声明读取权限 = 少一项隐私面、少一次授权弹窗。 -->

    <application
        android:name=".PixelCakeApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:enableOnBackInvokedCallback="true"
        android:hardwareAccelerated="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PixelCake">

        <!--
          不再声明 android:colorMode="hdr"（FIX_LIST F18）：管线是 8-bit sRGB，
          请求系统按广色域合成窗口并不带来任何实际收益，反而名不副实。
          等真正上 RGBA_F16 缓冲 + P3 输出时再打开。
        -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.PixelCake"
            android:windowSoftInputMode="adjustResize"
            android:screenOrientation="fullSensor"
            tools:ignore="DiscouragedApi"
            android:configChanges="colorMode|screenSize|smallestScreenSize|screenLayout|orientation|density|uiMode">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 调试日志导出：把 files/debug 下的日志通过系统分享发出去 -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>

</manifest>
